node_modules/
# Toute variante d'env — .env.bak, .env.local.old, .env.save… : un fichier de
# secrets sauvegardé à la main ne doit jamais pouvoir être commité par accident.
.env*
!.env.example
.DS_Store
web/public/media/
# Sources brutes déposées pour un import (2 Go de .MOV et .HEIC) : elles
# transitent par ingest/worker/import-folder.js vers R2, jamais par git.
Inspiration vrac/
*.MOV
*.HEIC
web/.next/
